// All time-related constants in one place to keep animation logic in sync.

export const TIMING = {
  LOADER_MS: 1700,
  CURTAIN_MS: 1000,
  HASH_SCROLL_DELAY_MS: 600,
  COOKIE_BANNER_DELAY_MS: 2000,
};

export const REVEAL = {
  THRESHOLD: 0.12,
  ROOT_MARGIN: "0px 0px -8% 0px",
};

export const STORAGE_KEYS = {
  COOKIE_CONSENT: "osecom_cookie_consent",
};
